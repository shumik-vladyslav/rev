import { FiltrListParamPipe } from './filtr-list-param.pipe';

describe('FiltrListParamPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrListParamPipe();
    expect(pipe).toBeTruthy();
  });
});
